<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chulangnghiem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ebook"
            android:configChanges="orientation|screenSize"
            android:label="Chú Lăng Nghiêm"
            android:parentActivityName=".Ebook"
            android:theme="@style/AppThemeGioiThieu">
        </activity>
        <activity android:name=".BangChiaCau"
        android:configChanges="orientation|screenSize"
        android:label="Chú Lăng Nghiêm"
        android:parentActivityName=".Ebook"
            android:theme="@style/AppThemeGioiThieu">
        </activity>
        <activity
            android:name=".GioiThieu"
            android:configChanges="orientation|screenSize"
            android:label="Chú Lăng Nghiêm"
            android:parentActivityName=".Ebook"
            android:theme="@style/AppThemeGioiThieu" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTask" />
        <activity
            android:name=".SplashScreenActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>